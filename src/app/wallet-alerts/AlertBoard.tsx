"use client";

import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";

type Alert = { id: string; asset: string; condition: string; value: string; enabled: boolean };
const storageKey = "cryptosbeginner-local-alerts";

export default function AlertBoard() {
  const [alerts, setAlerts] = useState<Alert[]>([]);
  const [asset, setAsset] = useState("BTC");
  const [condition, setCondition] = useState("above");
  const [value, setValue] = useState("");
  const [message, setMessage] = useState("");
  useEffect(() => { try { const raw = window.localStorage.getItem(storageKey); if (raw) window.setTimeout(() => setAlerts(JSON.parse(raw)), 0); } catch { /* optional */ } }, []);
  useEffect(() => { try { window.localStorage.setItem(storageKey, JSON.stringify(alerts)); } catch { /* optional */ } }, [alerts]);
  function add(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!asset.trim() || !value.trim()) return; setAlerts((current) => [{ id: crypto.randomUUID(), asset: asset.trim().toUpperCase(), condition, value: value.trim(), enabled: true }, ...current]); setValue(""); setMessage("Alert saved in this browser."); }
  return <main className="min-h-screen bg-[#f7f7fb] text-slate-950"><section className="relative overflow-hidden bg-[#0b0820] text-white"><div className="pointer-events-none absolute -left-16 top-0 h-72 w-72 rounded-full bg-amber-300/15 blur-3xl" /><div className="pointer-events-none absolute right-0 top-10 h-80 w-80 rounded-full bg-cyan-400/15 blur-3xl" /><div className="relative mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><Link href="/crypto-prices" className="text-sm font-black text-cyan-200 hover:text-white">← Back to market board</Link><p className="mt-10 text-xs font-black uppercase tracking-[0.2em] text-amber-200">Alert foundation · browser-local</p><h1 className="mt-4 max-w-4xl text-5xl font-black leading-[.98] tracking-[-0.055em] sm:text-7xl">Set the rule.<br /><span className="bg-gradient-to-r from-amber-200 via-white to-cyan-200 bg-clip-text text-transparent">Do not chase the ping.</span></h1><p className="mt-6 max-w-2xl text-base leading-8 text-slate-300 sm:text-lg">Create simple price-threshold reminders that stay in this browser. This demo does not run in the background or send email, Telegram, Discord, or push notifications.</p></div></section><div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="grid gap-6 lg:grid-cols-[.75fr_1.25fr] lg:items-start"><form onSubmit={add} className="rounded-[1.5rem] border border-slate-200 bg-white p-6 shadow-xl shadow-slate-200/40 sm:p-7"><p className="text-xs font-black uppercase tracking-[0.16em] text-amber-700">Local rule builder</p><h2 className="mt-2 text-2xl font-black">Add a threshold</h2><label className="mt-6 block text-xs font-black uppercase tracking-[0.14em] text-slate-500">Asset or symbol<input value={asset} onChange={(event) => setAsset(event.target.value)} placeholder="BTC or Bitcoin" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-semibold normal-case tracking-normal outline-none focus:border-amber-400 focus:ring-2 focus:ring-amber-100" /></label><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="block text-xs font-black uppercase tracking-[0.14em] text-slate-500">Condition<select value={condition} onChange={(event) => setCondition(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-black normal-case tracking-normal"><option value="above">Moves above</option><option value="below">Moves below</option></select></label><label className="block text-xs font-black uppercase tracking-[0.14em] text-slate-500">Price level<input value={value} onChange={(event) => setValue(event.target.value)} inputMode="decimal" placeholder="e.g. 100000" className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-semibold normal-case tracking-normal outline-none focus:border-amber-400 focus:ring-2 focus:ring-amber-100" /></label></div><button type="submit" className="mt-6 w-full rounded-xl bg-slate-950 px-4 py-3.5 text-sm font-black text-white hover:bg-amber-700">Save local alert</button>{message && <p className="mt-4 rounded-xl bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">{message}</p>}</form><section className="rounded-[1.5rem] border border-slate-200 bg-white p-6 shadow-xl shadow-slate-200/40 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-cyan-700">Your rules</p><h2 className="mt-2 text-2xl font-black">{alerts.length ? `${alerts.length} local alert${alerts.length === 1 ? "" : "s"}` : "No alerts yet"}</h2></div><span className="rounded-full bg-cyan-50 px-3 py-1.5 text-[11px] font-black text-cyan-800">No background process</span></div>{alerts.length ? <div className="mt-6 space-y-3">{alerts.map((alert) => <div key={alert.id} className="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-slate-50 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-black">{alert.asset} <span className="font-semibold text-slate-500">{alert.condition} {alert.value}</span></p><p className="mt-1 text-xs font-black uppercase tracking-wide text-slate-500">{alert.enabled ? "Enabled locally" : "Paused locally"}</p></div><div className="flex gap-2"><button type="button" onClick={() => setAlerts((current) => current.map((item) => item.id === alert.id ? { ...item, enabled: !item.enabled } : item))} className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs font-black text-slate-700">{alert.enabled ? "Pause" : "Enable"}</button><button type="button" onClick={() => setAlerts((current) => current.filter((item) => item.id !== alert.id))} className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs font-black text-rose-700">Remove</button></div></div>)}</div> : <div className="mt-6 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-10 text-sm leading-7 text-slate-600">Create a rule to preview the management experience. To send timely notifications later, the production version needs accounts, server-side storage, a scheduled data check, delivery credentials, and an explicit opt-in.</div>}</section></div><section className="mt-8 rounded-[1.5rem] border border-amber-200 bg-amber-50 p-6 sm:p-7"><p className="text-xs font-black uppercase tracking-[0.16em] text-amber-800">Production boundary</p><h2 className="mt-2 text-2xl font-black">Alerts are infrastructure, not advice</h2><p className="mt-3 max-w-4xl text-sm leading-7 text-amber-950/80">A notification can arrive late, trigger on a stale snapshot, or encourage impulsive action. A production alert service should show the source timestamp, throttle repeated notices, log delivery status, and let users pause or delete their rules. It should never describe a threshold crossing as a recommendation.</p></section></div></main>;
}
