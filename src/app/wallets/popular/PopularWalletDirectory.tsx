"use client";

import Link from "next/link";
import { useMemo, useState } from "react";
import { popularWallets, walletCategories, type WalletCategory } from "./wallet-data";

function categoryTone(category: WalletCategory) {
  if (category === "corporate") return "bg-indigo-100 text-indigo-950";
  if (category === "government") return "bg-amber-100 text-amber-950";
  if (category === "public-figure") return "bg-fuchsia-100 text-fuchsia-950";
  return "bg-cyan-100 text-cyan-950";
}

function providerTone(provider: string) { return provider === "Arkham" ? "border-violet-200 bg-violet-50 text-violet-950" : "border-blue-200 bg-blue-50 text-blue-950"; }

export default function PopularWalletDirectory() {
  const [category, setCategory] = useState<(typeof walletCategories)[number]["id"]>("all");
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => popularWallets.filter((wallet) => (category === "all" || wallet.category === category) && `${wallet.name} ${wallet.shortName} ${wallet.network} ${wallet.categoryLabel}`.toLowerCase().includes(query.trim().toLowerCase())), [category, query]);

  return <main className="min-h-screen bg-[#f7f7fb] text-slate-950"><section className="relative overflow-hidden bg-[#0b0820] text-white"><div className="pointer-events-none absolute -left-20 top-0 h-80 w-80 rounded-full bg-cyan-400/20 blur-3xl" /><div className="pointer-events-none absolute right-0 top-20 h-96 w-96 rounded-full bg-fuchsia-500/20 blur-3xl" /><div className="relative mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><Link href="/public-wallets" className="text-sm font-black text-cyan-200 hover:text-white">← Open your local wallet board</Link><div className="mt-10 grid gap-10 lg:grid-cols-[1.12fr_.88fr] lg:items-end"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-cyan-200">Public-wallet research directory · read-only</p><h1 className="mt-4 max-w-4xl text-5xl font-black leading-[.98] tracking-[-0.055em] sm:text-7xl">Track the label.<br /><span className="bg-gradient-to-r from-cyan-200 via-white to-fuchsia-300 bg-clip-text text-transparent">Question the story.</span></h1><p className="mt-6 max-w-2xl text-base leading-8 text-slate-300 sm:text-lg">A curated index of public crypto-wallet profiles linked to company disclosures, government records, or analytics-provider labels. It is an observation tool—not a trade signal, ownership registry, or copy-trading service.</p><div className="mt-7 flex flex-wrap gap-3"><Link href="/wallet-tracker" className="rounded-xl bg-cyan-300 px-5 py-3 text-sm font-black text-slate-950 transition hover:bg-white">Check another public address</Link><Link href="/methodology" className="rounded-xl border border-white/20 bg-white/10 px-5 py-3 text-sm font-black text-white transition hover:bg-white/15">Read methodology</Link></div></div><aside className="rounded-[2rem] border border-white/15 bg-white/[.08] p-6 backdrop-blur-xl"><p className="text-xs font-black uppercase tracking-[0.16em] text-fuchsia-200">Attribution rule</p><p className="mt-4 text-2xl font-black leading-tight">A provider label is a lead, not a verdict.</p><p className="mt-4 text-sm leading-7 text-slate-300">Address clustering and entity names can change. Corporate and government records are presented alongside—not replaced by—provider snapshots.</p><div className="mt-6 grid grid-cols-3 gap-2 text-center"><div className="rounded-xl bg-white/10 p-3"><p className="text-2xl font-black">{popularWallets.length}</p><p className="mt-1 text-[10px] font-black uppercase tracking-wide text-slate-300">profiles</p></div><div className="rounded-xl bg-white/10 p-3"><p className="text-2xl font-black">{new Set(popularWallets.map((wallet) => wallet.provider)).size}</p><p className="mt-1 text-[10px] font-black uppercase tracking-wide text-slate-300">providers</p></div><div className="rounded-xl bg-white/10 p-3"><p className="text-2xl font-black">{new Set(popularWallets.map((wallet) => wallet.network)).size}</p><p className="mt-1 text-[10px] font-black uppercase tracking-wide text-slate-300">views</p></div></div></aside></div></div></section><div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><section className="rounded-[1.75rem] border border-slate-200 bg-white p-5 shadow-xl shadow-slate-200/40 sm:p-7"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-fuchsia-700">Explore the directory</p><h2 className="mt-2 text-3xl font-black tracking-tight">Profiles with a provenance trail</h2><p className="mt-2 max-w-2xl text-sm leading-7 text-slate-600">Filter by the type of evidence behind the profile. The label shown on each card is part of the evidence record, not a guarantee of control.</p></div><label className="block w-full lg:max-w-xs"><span className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">Search profiles</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Try Strategy, Bitcoin, Arkham…" className="mt-2 w-full rounded-xl border border-slate-300 bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-900 outline-none focus:border-fuchsia-500 focus:ring-2 focus:ring-fuchsia-100" /></label></div><div className="mt-6 flex gap-2 overflow-x-auto pb-1">{walletCategories.map((item) => <button key={item.id} type="button" onClick={() => setCategory(item.id)} className={`shrink-0 rounded-full px-4 py-2 text-sm font-black transition ${category === item.id ? "bg-slate-950 text-white" : "border border-slate-300 bg-white text-slate-700 hover:border-fuchsia-400 hover:text-slate-950"}`}>{item.label}</button>)}</div><div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map((wallet) => <article key={wallet.slug} className="group flex flex-col rounded-[1.5rem] border border-slate-200 bg-slate-50 p-5 transition hover:-translate-y-0.5 hover:border-fuchsia-300 hover:bg-white hover:shadow-lg"><div className="flex items-start justify-between gap-3"><span className={`rounded-full px-3 py-1 text-[10px] font-black uppercase tracking-[0.14em] ${categoryTone(wallet.category)}`}>{wallet.categoryLabel}</span><span className={`rounded-full border px-2.5 py-1 text-[10px] font-black ${providerTone(wallet.provider)}`}>{wallet.provider}</span></div><h3 className="mt-5 text-2xl font-black tracking-tight text-slate-950">{wallet.name}</h3><p className="mt-2 text-sm font-bold text-slate-700">{wallet.network}</p><p className="mt-4 text-sm leading-7 text-slate-700">{wallet.summary}</p>{wallet.snapshot ? <div className="mt-5 rounded-xl border border-slate-200 bg-white p-4"><p className="text-[10px] font-black uppercase tracking-[0.14em] text-slate-500">Provider snapshot · {wallet.snapshot.asOf}</p><p className="mt-2 text-2xl font-black text-slate-950">{wallet.snapshot.value}</p><p className="mt-1 text-xs font-semibold text-slate-600">{wallet.snapshot.detail}</p></div> : <div className="mt-5 rounded-xl border border-dashed border-slate-300 bg-white p-4"><p className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">Snapshot status</p><p className="mt-2 text-sm font-bold text-slate-800">Open the current provider profile</p><p className="mt-1 text-xs leading-5 text-slate-600">No volatile balance is hardcoded for this record.</p></div>}<div className="mt-5 flex items-center justify-between gap-3 border-t border-slate-200 pt-4"><span className="text-xs font-black text-slate-600">{wallet.attribution}</span><Link href={wallet.providerUrl} target="_blank" rel="noopener noreferrer" className="text-sm font-black text-indigo-900 hover:underline">Open source ↗</Link></div><Link href={`/wallets/popular/${wallet.slug}`} className="mt-4 rounded-xl bg-slate-950 px-4 py-3 text-center text-sm font-black text-white transition hover:bg-fuchsia-700">View research profile →</Link></article>)}</div>{!filtered.length && <div className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-10 text-center"><p className="font-black">No profiles match that search.</p><button type="button" onClick={() => { setCategory("all"); setQuery(""); }} className="mt-3 text-sm font-black text-indigo-800 hover:underline">Clear filters</button></div>}</section><section className="mt-8 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-[1.75rem] border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-black uppercase tracking-[0.16em] text-cyan-700">How to read the cards</p><h2 className="mt-2 text-2xl font-black">Three layers of evidence</h2><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-slate-950 p-4 text-white"><p className="text-xs font-black text-cyan-200">01 · Primary</p><p className="mt-2 text-sm font-black">Company, government, or court source</p></div><div className="rounded-xl bg-cyan-50 p-4"><p className="text-xs font-black text-cyan-800">02 · Provider</p><p className="mt-2 text-sm font-black text-slate-950">Arkham or CoinStats entity label</p></div><div className="rounded-xl bg-fuchsia-50 p-4"><p className="text-xs font-black text-fuchsia-800">03 · Interpretation</p><p className="mt-2 text-sm font-black text-slate-950">CryptosBeginner’s limits and context</p></div></div></div><div className="rounded-[1.75rem] border border-amber-200 bg-amber-50 p-6"><p className="text-xs font-black uppercase tracking-[0.16em] text-amber-800">Meme-coin mode</p><h2 className="mt-2 text-2xl font-black text-amber-950">Track roles, not rumors</h2><p className="mt-3 text-sm leading-7 text-amber-950">For meme coins, the useful records are deployer, treasury, fee-recipient, liquidity-pool, burn, vesting, and official multisig wallets. A large holder is not automatically a team wallet or a reliable signal.</p><Link href="/meme-coins/guides/token-verification" className="mt-4 inline-block text-sm font-black text-amber-950 underline">Read the token-verification guide →</Link></div></section></div></main>;
}
