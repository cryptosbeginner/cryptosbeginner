"use client";

import Image from "next/image";
import Link from "next/link";
import { useMemo, useState } from "react";
import { cardCategories, cardListings, CardCategory, CardListing } from "./cards-data";

function CardArtwork({ card, large = false }: { card: CardListing; large?: boolean }) {
  const [broken, setBroken] = useState(false);
  return (
    <div className={`relative overflow-hidden rounded-[1.5rem] bg-[radial-gradient(circle_at_20%_15%,rgba(255,255,255,.34),transparent_30%),linear-gradient(135deg,#111827,#312e81_58%,#c026d3)] ${large ? "min-h-[270px]" : "min-h-[190px]"}`}>
      {!broken && <Image src={card.imageUrl} alt={card.imageAlt} onError={() => setBroken(true)} fill priority={large} sizes="(max-width: 768px) 100vw, (max-width: 1280px) 50vw, 33vw" className="object-contain p-5 mix-blend-normal" referrerPolicy="no-referrer" />}
      {broken && <div className="absolute inset-0 flex flex-col justify-between p-6 text-white"><span className="text-xs font-black uppercase tracking-[0.18em] text-cyan-200">Official product link</span><div><p className="text-4xl font-black tracking-[-0.06em]">{card.name.split(" ").slice(0, 2).join(" ")}</p><p className="mt-2 text-sm font-semibold text-white/75">Image unavailable in this environment. View the official source for the current design.</p></div></div>}
      <span className="absolute left-4 top-4 rounded-full border border-white/20 bg-slate-950/70 px-3 py-1.5 text-[10px] font-black uppercase tracking-[0.13em] text-white">{card.network}</span>
    </div>
  );
}

function categoryLabel(category: CardCategory) { return category === "dual-mode" ? "Dual-mode" : category === "self-custody" ? "Self-custody" : category[0].toUpperCase() + category.slice(1); }

export default function CardsDirectory() {
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState<"all" | CardCategory>("all");
  const [region, setRegion] = useState("all");
  const [sort, setSort] = useState<"name" | "reviewed">("name");

  const filtered = useMemo(() => {
    const needle = query.trim().toLowerCase();
    return [...cardListings]
      .filter((card) => !needle || [card.name, card.provider, card.network, card.shortDescription, card.bestFor].join(" ").toLowerCase().includes(needle))
      .filter((card) => category === "all" || card.categories.includes(category))
      .filter((card) => region === "all" || card.regions.some((item) => item.toLowerCase().includes(region.toLowerCase())))
      .sort((a, b) => sort === "name" ? a.name.localeCompare(b.name) : b.reviewedAt.localeCompare(a.reviewedAt));
  }, [category, query, region, sort]);

  return <main className="min-h-screen bg-[#f7f7fb] text-slate-950"><section className="relative overflow-hidden bg-[#0b0820] text-white"><div className="pointer-events-none absolute -left-16 top-0 h-80 w-80 rounded-full bg-fuchsia-500/20 blur-3xl" /><div className="pointer-events-none absolute right-0 top-12 h-96 w-96 rounded-full bg-cyan-400/15 blur-3xl" /><div className="relative mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><div className="flex flex-wrap items-center justify-between gap-4"><Link href="/" className="text-sm font-black text-cyan-200 hover:text-white">← Back to CryptosBeginner</Link><span className="rounded-full border border-white/15 bg-white/[.08] px-3 py-1.5 text-[11px] font-black uppercase tracking-[0.14em] text-fuchsia-100">Directory demo · reviewed {cardListings[0].reviewedAt}</span></div><div className="mt-12 grid gap-10 lg:grid-cols-[1fr_.78fr] lg:items-end"><div><p className="text-xs font-black uppercase tracking-[0.22em] text-cyan-200">Crypto Cards · compare the rails</p><h1 className="mt-4 max-w-4xl text-5xl font-black leading-[.96] tracking-[-0.06em] sm:text-7xl">Find the card.<br /><span className="bg-gradient-to-r from-cyan-200 via-white to-fuchsia-300 bg-clip-text text-transparent">Read the footnotes.</span></h1><p className="mt-6 max-w-2xl text-base leading-8 text-slate-300 sm:text-lg">A source-backed directory of crypto spending cards, from prepaid exchange products to wallet-linked and collateral-backed models. Availability, fees, rewards, issuers, and taxes vary; this page organizes the facts without turning them into a personal recommendation.</p><div className="mt-7 flex flex-wrap gap-3"><a href="#directory" className="rounded-xl bg-white px-5 py-3 text-sm font-black text-slate-950 transition hover:-translate-y-0.5">Browse cards</a><a href="#how-we-list" className="rounded-xl border border-white/20 bg-white/[.08] px-5 py-3 text-sm font-black text-white transition hover:bg-white/15">How this directory works</a></div></div><div className="rounded-[2rem] border border-white/15 bg-white/[.08] p-6 backdrop-blur-xl"><p className="text-xs font-black uppercase tracking-[0.17em] text-fuchsia-200">The fast answer</p><p className="mt-4 text-2xl font-black tracking-[-0.04em]">A crypto card is not one product category.</p><p className="mt-3 text-sm leading-7 text-slate-300">Some cards spend a prepaid balance, some convert crypto at checkout, some link to self-custody, and some let you borrow against collateral. Compare the funding model before comparing the cashback headline.</p></div></div></div></section><section id="directory" className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="rounded-[1.75rem] border border-slate-200 bg-white p-5 shadow-xl shadow-slate-200/30 sm:p-7"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.17em] text-fuchsia-700">Directory controls</p><h2 className="mt-2 text-3xl font-black tracking-[-0.04em]">Compare by product shape</h2></div><p className="text-sm font-semibold text-slate-500">{filtered.length} of {cardListings.length} demo listings</p></div><div className="mt-6 grid gap-3 md:grid-cols-[1.35fr_.8fr_.8fr_.65fr]"><label className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">Search<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Provider, card, network, use case" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-semibold normal-case tracking-normal outline-none focus:border-fuchsia-400 focus:ring-2 focus:ring-fuchsia-100" /></label><label className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">Model<select value={category} onChange={(event) => setCategory(event.target.value as "all" | CardCategory)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-black normal-case tracking-normal"><option value="all">All approaches</option>{cardCategories.slice(1).map((item) => <option key={item.value} value={item.value}>{item.label}</option>)}</select></label><label className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">Region<select value={region} onChange={(event) => setRegion(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-black normal-case tracking-normal"><option value="all">Any listed region</option><option value="United States">United States</option><option value="Europe">Europe / EEA</option><option value="United Kingdom">United Kingdom</option><option value="150+">150+ countries advertised</option></select></label><label className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">Order<select value={sort} onChange={(event) => setSort(event.target.value as "name" | "reviewed")} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-sm font-black normal-case tracking-normal"><option value="name">Name</option><option value="reviewed">Recently reviewed</option></select></label></div></div><div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map((card) => <article key={card.slug} className="group overflow-hidden rounded-[1.5rem] border border-slate-200 bg-white shadow-lg shadow-slate-200/25 transition duration-200 hover:-translate-y-1 hover:border-fuchsia-200 hover:shadow-xl"><CardArtwork card={card} /><div className="p-5"><div className="flex flex-wrap gap-2">{card.categories.map((item) => <span key={item} className="rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-black uppercase tracking-[0.12em] text-slate-600">{categoryLabel(item)}</span>)}</div><div className="mt-4 flex items-start justify-between gap-3"><div><h3 className="text-xl font-black tracking-[-0.03em]">{card.name}</h3><p className="mt-1 text-xs font-black uppercase tracking-[0.13em] text-slate-500">{card.provider}</p></div><span className="rounded-full bg-cyan-50 px-2.5 py-1 text-[10px] font-black text-cyan-800">{card.cardFormats[0]}</span></div><p className="mt-4 text-sm leading-6 text-slate-600">{card.shortDescription}</p><div className="mt-5 grid gap-3 border-y border-slate-100 py-4 text-xs"><div><p className="font-black uppercase tracking-[0.12em] text-slate-400">Pricing</p><p className="mt-1 leading-5 text-slate-700">{card.pricing}</p></div><div><p className="font-black uppercase tracking-[0.12em] text-slate-400">Availability</p><p className="mt-1 leading-5 text-slate-700">{card.availability}</p></div></div><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><span className="text-xs font-semibold text-slate-500">Reviewed {card.reviewedAt}</span><Link href={`/crypto-cards/${card.slug}`} className="rounded-xl bg-slate-950 px-3 py-2.5 text-xs font-black text-white transition hover:bg-fuchsia-700">Open profile →</Link></div></div></article>)}</div>{filtered.length === 0 && <div className="mt-7 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-10 text-center"><p className="text-lg font-black">No listing matches those filters.</p><p className="mt-2 text-sm text-slate-600">Try a broader search or reset one filter; an empty result is more honest than a forced recommendation.</p></div>}</section><section id="how-we-list" className="mx-auto max-w-7xl px-4 pb-14 sm:px-6 lg:px-8"><div className="grid gap-5 lg:grid-cols-[.95fr_1.05fr]"><div className="rounded-[1.75rem] bg-slate-950 p-7 text-white"><p className="text-xs font-black uppercase tracking-[0.17em] text-cyan-200">Editorial method</p><h2 className="mt-3 text-3xl font-black tracking-[-0.04em]">What makes a listing useful?</h2><p className="mt-4 text-sm leading-7 text-slate-300">Each profile separates the card’s funding model, network, formats, region wording, pricing, rewards, and provider relationship. Facts are checked against the linked official product page; they are not copied into a fake score.</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[.06] p-4"><p className="text-xs font-black text-fuchsia-200">01 · Source</p><p className="mt-2 text-sm leading-6 text-slate-300">The provider page is the evidence layer.</p></div><div className="rounded-2xl border border-white/10 bg-white/[.06] p-4"><p className="text-xs font-black text-cyan-200">02 · Scope</p><p className="mt-2 text-sm leading-6 text-slate-300">Country and tier conditions stay visible.</p></div><div className="rounded-2xl border border-white/10 bg-white/[.06] p-4"><p className="text-xs font-black text-amber-200">03 · Costs</p><p className="mt-2 text-sm leading-6 text-slate-300">Fees, lockups, spreads, and funding are distinct.</p></div><div className="rounded-2xl border border-white/10 bg-white/[.06] p-4"><p className="text-xs font-black text-emerald-200">04 · Update</p><p className="mt-2 text-sm leading-6 text-slate-300">Review dates make stale copy easier to spot.</p></div></div></div><div className="rounded-[1.75rem] border border-amber-200 bg-amber-50 p-7"><p className="text-xs font-black uppercase tracking-[0.17em] text-amber-800">Read before applying</p><h2 className="mt-3 text-3xl font-black tracking-[-0.04em] text-amber-950">Headline rewards are not the whole cost.</h2><p className="mt-4 text-sm leading-7 text-amber-950/80">A card may involve identity verification, a spread, FX conversion, staking or lockup requirements, borrowing risk, tax consequences, ATM charges, or an issuer that is different from the brand on the front. Availability can change without the directory changing at the same time.</p><Link href="/methodology" className="mt-6 inline-flex rounded-xl bg-amber-950 px-4 py-3 text-sm font-black text-white hover:bg-amber-900">Read site methodology →</Link></div></div></section></main>;
}
