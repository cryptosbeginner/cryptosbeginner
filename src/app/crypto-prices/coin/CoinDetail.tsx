import Link from "next/link";
import type { MarketCoin, MarketCurrency } from "../../api/crypto/markets/market-data";
import MarketIcon from "@/components/MarketIcon";
import { getProjectProfile } from "./project-profiles";

function money(value: number | null, currency: MarketCurrency = "usd", compact = false) {
  if (value === null) return "—";
  return new Intl.NumberFormat("en-US", { style: "currency", currency: currency.toUpperCase(), notation: compact ? "compact" : "standard", maximumFractionDigits: value < 1 ? 6 : compact ? 1 : 2 }).format(value);
}
function pct(value: number | null) { return value === null ? "—" : `${value >= 0 ? "+" : ""}${value.toFixed(2)}%`; }
function pctTone(value: number | null) { return value !== null && value >= 0 ? "text-emerald-700" : "text-rose-700"; }

export default function CoinDetail({ coin, related, provider, checkedAt }: { coin: MarketCoin; related: MarketCoin[]; provider: string; checkedAt: string }) {
  const periods = [["1 hour", coin.change1h], ["24 hours", coin.change24h], ["7 days", coin.change7d], ["30 days", coin.change30d]] as const;
  const profile = getProjectProfile(coin.id, coin.name, coin.symbol);
  const checked = new Date(checkedAt);
  const checkedLabel = Number.isNaN(checked.getTime()) ? "recently checked" : checked.toLocaleString([], { dateStyle: "medium", timeStyle: "short" });
  return <main className="min-h-screen bg-[#f7f7fb] text-slate-950"><section className="relative overflow-hidden bg-[#0b0820] text-white"><div className="pointer-events-none absolute -left-16 top-0 h-72 w-72 rounded-full bg-cyan-400/20 blur-3xl" /><div className="pointer-events-none absolute right-0 top-10 h-80 w-80 rounded-full bg-fuchsia-500/20 blur-3xl" /><div className="relative mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><Link href="/crypto-prices" className="text-sm font-black text-cyan-200 hover:text-white">← Back to live prices</Link><div className="mt-10 flex flex-col gap-7 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-5"><MarketIcon src={coin.image} name={coin.name} symbol={coin.symbol} size="lg" /><div><p className="text-xs font-black uppercase tracking-[0.18em] text-cyan-200">Coin research card · #{coin.rank}</p><h1 className="mt-2 text-4xl font-black tracking-[-0.05em] sm:text-6xl">{coin.name}</h1><p className="mt-2 text-lg font-black uppercase tracking-wide text-slate-300">{coin.symbol} · {provider} snapshot</p></div></div><div className="rounded-2xl border border-white/15 bg-white/[.08] px-5 py-4 backdrop-blur-xl"><p className="text-xs font-black uppercase tracking-[0.14em] text-slate-400">Current price</p><p className="mt-1 text-3xl font-black">{money(coin.price)}</p><p className={`mt-1 text-sm font-black ${coin.change24h !== null && coin.change24h >= 0 ? "text-emerald-300" : "text-rose-300"}`}>{pct(coin.change24h)} in 24h</p></div></div></div></section><div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[0.2em] text-fuchsia-700">Snapshot context</p><h2 className="mt-2 text-3xl font-black tracking-[-0.04em]">How the move looks across time</h2></div><p className="text-xs font-semibold text-slate-500">Checked {checkedLabel} · stale window is provider-dependent</p></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{periods.map(([label, value]) => <div key={label} className="rounded-[1.5rem] border border-slate-200 bg-white p-5 shadow-lg shadow-slate-200/30"><p className="text-xs font-black uppercase tracking-[0.14em] text-slate-500">{label}</p><p className={`mt-3 text-2xl font-black ${pctTone(value)}`}>{pct(value)}</p><p className="mt-2 text-xs leading-5 text-slate-500">Provider-reported percentage change. It is not a forecast.</p></div>)}</div><section className="mt-5 rounded-[1.5rem] border border-cyan-100 bg-cyan-50/70 p-6 shadow-lg shadow-cyan-100/30 sm:p-8"><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-cyan-800">Project profile · {profile.profileStatus === "curated" ? "curated overview" : "market context"}</p><h2 className="mt-2 text-3xl font-black tracking-[-0.04em]">What is {coin.name}?</h2></div>{profile.officialUrl ? <a href={profile.officialUrl} target="_blank" rel="noreferrer" className="inline-flex items-center rounded-full bg-slate-950 px-4 py-2.5 text-xs font-black text-white hover:bg-cyan-700">{profile.officialLabel ?? "Official website"} ↗</a> : <span className="rounded-full border border-cyan-200 bg-white/70 px-4 py-2.5 text-xs font-black text-cyan-900">Official link pending verification</span>}</div><div className="mt-6 grid gap-5 md:grid-cols-3"><div><p className="text-xs font-black uppercase tracking-[0.14em] text-cyan-800">Overview</p><p className="mt-2 text-sm leading-7 text-slate-700">{profile.overview}</p></div><div><p className="text-xs font-black uppercase tracking-[0.14em] text-cyan-800">History</p><p className="mt-2 text-sm leading-7 text-slate-700">{profile.history}</p></div><div><p className="text-xs font-black uppercase tracking-[0.14em] text-cyan-800">What to check now</p><p className="mt-2 text-sm leading-7 text-slate-700">{profile.updates}</p></div></div><p className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-950"><strong>Context warning.</strong> {profile.caution}</p></section><div className="mt-5 grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-[1.5rem] border border-slate-200 bg-white p-6 shadow-lg shadow-slate-200/30 sm:p-8"><p className="text-xs font-black uppercase tracking-[0.16em] text-cyan-700">Market profile</p><h2 className="mt-2 text-2xl font-black">The numbers behind the headline</h2><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-black uppercase tracking-wide text-slate-500">Market cap</p><p className="mt-2 text-xl font-black">{money(coin.marketCap, "usd", true)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-black uppercase tracking-wide text-slate-500">24h volume</p><p className="mt-2 text-xl font-black">{money(coin.volume24h, "usd", true)}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-black uppercase tracking-wide text-slate-500">Market rank</p><p className="mt-2 text-xl font-black">#{coin.rank}</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-black uppercase tracking-wide text-slate-500">Provider ID</p><p className="mt-2 truncate text-sm font-black">{coin.id}</p></div></div><p className="mt-6 text-sm leading-7 text-slate-600">Market cap and volume help describe the scale and recent activity represented in this snapshot. They do not tell you how easily a specific order will execute, how concentrated ownership is, or whether a contract is secure.</p></section><section className="rounded-[1.5rem] border border-amber-200 bg-amber-50 p-6 shadow-lg shadow-amber-100/30 sm:p-8"><p className="text-xs font-black uppercase tracking-[0.16em] text-amber-800">Research checklist</p><h2 className="mt-2 text-2xl font-black">Before you trust a token</h2><div className="mt-5 space-y-4 text-sm leading-6 text-amber-950/80"><p><strong className="text-amber-950">Confirm the identity.</strong> Match the project name, ticker, contract address, and chain. Similar symbols can represent unrelated assets.</p><p><strong className="text-amber-950">Check the market.</strong> Look beyond a percentage move: liquidity, spread, volume quality, holder concentration, and venue differences matter.</p><p><strong className="text-amber-950">Read the contract context.</strong> A market-data feed is not an audit and cannot rule out minting, freezing, blacklist, or honeypot behaviour.</p></div><Link href="/meme-coin-research" className="mt-6 inline-flex items-center gap-2 text-sm font-black text-amber-900 hover:text-amber-700">Open token research guide →</Link></section></div>{related.length > 0 && <section className="mt-12"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[0.16em] text-fuchsia-700">Nearby market context</p><h2 className="mt-2 text-2xl font-black">More assets to compare</h2></div><Link href="/crypto-screener" className="text-sm font-black text-fuchsia-700 hover:text-fuchsia-900">Open screener →</Link></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{related.map((item) => <Link key={item.id} href={`/crypto-prices/coin/${encodeURIComponent(item.id)}`} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-1 hover:border-fuchsia-200"><div className="flex items-center gap-3"><MarketIcon src={item.image} name={item.name} symbol={item.symbol} size="sm" /><div className="min-w-0"><p className="truncate font-black">{item.name}</p><p className="text-xs font-black uppercase text-slate-500">#{item.rank} · {item.symbol}</p></div></div><p className={`mt-4 text-sm font-black ${pctTone(item.change24h)}`}>{pct(item.change24h)} <span className="font-semibold text-slate-400">24h</span></p></Link>)}</div></section>}<section className="mt-12 rounded-[1.5rem] border border-slate-200 bg-white p-6 text-sm leading-7 text-slate-600 shadow-sm"><strong className="text-slate-950">Data note.</strong> This page describes a time-stamped provider response. It is not an exchange order book, execution quote, investment recommendation, or guarantee of future performance. If a provider is delayed or unavailable, the page may show an older snapshot or no result.</section></div></main>;
}
