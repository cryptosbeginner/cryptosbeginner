import { notFound } from "next/navigation";
import { platforms, videoSources } from "../../content";
import { PlatformProfile } from "../../MemeArticle";
import type { Metadata } from "next";
export function generateStaticParams() { return platforms.map((p) => ({ slug: p.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const p = platforms.find((x) => x.slug === slug); return p ? { title: `${p.name} Review 2026: Features, Fees & Risk Checks`, description: `${p.summary} Review fee layers, custody, limitations, and practical checks before using ${p.name}.`, alternates: { canonical: `https://www.cryptosbeginner.com/meme-coins/reviews/${p.slug}` }, openGraph: { title: `${p.name} Review 2026: Features, Fees & Risk Checks`, description: `${p.summary} Review fee layers, custody, limitations, and practical checks before using ${p.name}.`, url: `https://www.cryptosbeginner.com/meme-coins/reviews/${p.slug}`, type: "article" } } : {}; }
export default async function ReviewPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const p = platforms.find((x) => x.slug === slug); if (!p) notFound();
  if (p.slug === "gmgn-ai") return <PlatformProfile platform={p}><h2>What GMGN shows</h2><p>The supplied videos show Trenches and Trending discovery, token cards, chart timeframes, RSI and volume indicators, bubble maps, holders, token-audit surfaces, developer history, presets, Snipe X, Copywallet, and Buy/Sell panels. These features organize evidence; they do not certify a token.</p><h2>What to check before copy trading</h2><p>Copy-style workflows add latency, selection bias, timing mismatch, position-size mismatch, and an exit problem. Treat the wallet’s P&L as historical context. Ask whether the result is realized, what period it covers, and whether you can independently verify the token and liquidity.</p><h2>Video-informed workflow</h2><p>The videos demonstrate starting in Trenches, filtering by age, market cap, volume, transactions, and total fees, then opening the token detail page before checking holders, shared funding, developer history, and chart context. Thresholds mentioned by the narrator are heuristics, not universal safety tests. Low displayed fees can be a reason to investigate, but they are not technical proof of a scam.</p><h2>Fee stack</h2><p>GMGN’s official documentation states a 1% handling fee per single transaction. Gas, priority fees, tips, venue fees, launchpad fees, slippage, and price impact remain separate layers. Check the live settings before relying on a number.</p><h2>Who should avoid GMGN</h2><p>A reader looking for an automatic safe-token signal, a guaranteed fill, or a way to copy a high-performing wallet without understanding timing and risk should avoid using it as a shortcut.</p><h2>Video references</h2>{videoSources.filter((v) => v.platform === "GMGN.ai").map((v) => <p key={v.id}><a href={v.url} target="_blank" rel="noopener noreferrer">Open the original GMGN video on YouTube ↗</a> — {v.caveat}</p>)}</PlatformProfile>;
  if (p.slug === "fomo-photon") return <PlatformProfile platform={p}><h2>FOMO: social discovery and alerts</h2><p>The supplied FOMO video shows Trending and Leaderboard discovery, Thesis posts on charts, trader profiles with average entry, investment, transactions, P&L, and hold time, Holders and Swaps tabs, followed-trader alerts, and Buy/Sell controls. It also shows filters such as My swaps, Friends only, and Min size. The video’s visible “0% fee” label should not be treated as an all-in cost statement; network and route costs require separate verification.</p><h2>Photon: Solana discovery and quick trading</h2><p>Photon’s public product positioning focuses on Solana token discovery, monitoring, quick buy and sell, and wallet connection. Its displayed gains are provider examples, not expected results. Use the supplied token-detail, MemeScope, settings, and XTracker screenshots to explain the visible surfaces without turning them into performance evidence.</p><h2>Social proof is not due diligence</h2><p>The FOMO video explicitly warns against blindly copy-trading and notes that some traders may farm their followers. Use social feeds and alerts to form a research queue, then independently verify the contract, holders, liquidity, sellability, and wallet permissions.</p><h2>Video references</h2><p><a href="https://www.youtube.com/watch?v=PA4GoJvc5ls" target="_blank" rel="noopener noreferrer">Open the original FOMO video on YouTube ↗</a> — the observed workflow is useful as a feature demonstration, while its social and fee implications require independent checks.</p></PlatformProfile>;
  if (p.slug === "pump-fun") return <PlatformProfile platform={p}><h2>How pump.fun works</h2><p>pump.fun lowers the technical barrier to creating and trading tokens through a Solana launch flow. Graduated coins can be associated with canonical PumpSwap pools. A launch list or visible activity is not token due diligence.</p><h2>Current documented fee structure</h2><p>The official fee page lists 0 SOL for coin creation, 0.015 SOL when a coin graduates to PumpSwap, and separate creator, protocol, and LP fee layers. The listed bonding-curve schedule totals 1.25%, while PumpSwap canonical-pool rates vary by market-cap band. Wallet and network charges can be external, and pump.fun states its fees may change.</p><h2>Who should avoid pump.fun</h2><p>Readers who cannot tolerate a complete loss, cannot verify a contract, or are treating a launchpad ranking as proof of legitimacy should not use permissionless token creation as a shortcut.</p></PlatformProfile>;
  if (p.slug === "padre-terminal") return <PlatformProfile platform={p}><h2>What is verified</h2><p>The public landing page presents a trading terminal and advertises “up to 35% cashback,” execution speed, and control. Those statements are marketing claims, not independently verified outcomes. Before publication, check the current terms, eligible jurisdictions, fee basis, and cashback conditions.</p><h2>What the supplied interface shows</h2><p>The screenshots show a homepage, terminal interface, tracking surface, and Trenches view. They support feature descriptions but not connected execution tests, uptime claims, fee savings, or performance claims.</p><h2>Who should avoid it</h2><p>A reader who needs a guaranteed fee, a guaranteed fill, or a platform that removes smart-contract and liquidity risk should avoid treating the terminal as a safety layer.</p></PlatformProfile>;
  return <PlatformProfile platform={p}><h2>What {p.name} is for</h2><p>{p.summary} The platform should be evaluated as a workflow tool, not as a token-validation service.</p><h2>Features and limitations</h2><p>{p.caution}</p><h2>Fees and real execution cost</h2><p>{p.fee} Check the current provider documentation and the transaction receipt for the actual cost of any trade.</p><h2>First-use checklist</h2><p>Use a dedicated low-balance wallet, verify the contract from an independent source, inspect holders and liquidity, set an explicit slippage ceiling, test a small transaction when appropriate, and write an exit rule before signing.</p></PlatformProfile>;
}
